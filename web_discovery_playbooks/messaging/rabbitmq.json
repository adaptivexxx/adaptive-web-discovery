{
  "schema_version": 1,
  "id": "rabbitmq",
  "name": "Rabbitmq",
  "category": "messaging",
  "description": "Evidence and authorized read-only HTTP discovery guidance for rabbitmq.",
  "risk": "medium",
  "explicit_only": false,
  "protocols": [
    "http",
    "https",
    "amqp"
  ],
  "typical_ports": [
    15672
  ],
  "port_hints": [
    {"port": 15672, "role": "RabbitMQ management HTTP API and UI", "protocol": "http", "web": true, "scheme": "http"},
    {"port": 15671, "role": "RabbitMQ management HTTPS API and UI", "protocol": "https", "web": true, "scheme": "https"},
    {"port": 5672, "role": "RabbitMQ AMQP", "protocol": "amqp", "web": false},
    {"port": 5671, "role": "RabbitMQ AMQP TLS", "protocol": "amqp-tls", "web": false},
    {"port": 25672, "role": "RabbitMQ inter-node communication", "protocol": "tcp", "web": false}
  ],
  "signatures": [
    "rabbitmq",
    "rabbitmq_management"
  ],
  "paths": [
    "api/overview",
    "api/health/checks/alarms",
    "api/nodes",
    "api/queues",
    "metrics"
  ],
  "automatic_actions": [
    "Collect bounded HTTP response evidence.",
    "Record authentication boundaries and non-success statuses.",
    "Prioritize these routes before broad wordlist enumeration.",
    "Review management-plane exposure without publishing, consuming, or altering messages."
  ],
  "follow_up": [
    "Review virtual-host, topic, queue, ACL, and management-plane exposure using approved credentials."
  ],
  "stop_conditions": [
    "Stop on explicit scope mismatch.",
    "Stop or reduce request rate when throttling or instability is detected.",
    "Do not perform mutations, authentication bypass, or destructive actions."
  ]
}
